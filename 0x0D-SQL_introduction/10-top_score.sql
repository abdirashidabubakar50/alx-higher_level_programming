-- A script  that lists all records of the table second_table of the database hbtn_0c_0


-- listing all records of the table second_table in descending order
SELECT score, name
FROM second_table
ORDER BY score DESC;
